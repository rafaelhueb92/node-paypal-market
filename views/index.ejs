<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Créditos Choappeira</title>
        <style>
        
        </style>
    </head>
    <body>
        <div><h2>
            Pacotes de Crédito
        </h2></div>
        <br><br>
        <div>
            <% for(var i=0; i<credits.length;i++){ %>
                <div>
                    <h2><%= credits[i].titulo %></h2>
                    <h2><%= credits[i].descricao %></h2>
                    <h3><%= credits[i].preco %></h3>
                    <h3><%= credits[i].credito %></h3>
                </div>

                <form action="/buy?id=<%= credits[i].id %>" method="POST">
                        <input type="submit" value="Compre">
                </form>

                    <%  } %>
        </div>
    </body>
</html>

